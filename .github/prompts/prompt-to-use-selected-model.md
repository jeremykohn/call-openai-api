Generate a specification for adding the following feature to the Nuxt app in this repo:

- When the user submits a prompt, the model that is selected in the dropdown menu should be the model that is used to generate a response to the prompt.

Save the specification to .github/specs/application-specifications-use-selected-model.md.
